根据你目前的核心代码进度与阿里云 FC 的架构转向，这里为你整理了一份简洁的**全员待完善清单**，明确各成员的交付边界：

---

## 🚀 行研雷达：全员待办清单 (To-Do List)

### **成员 A：架构与基础设施 (Backend & Ops)**
关键：定时触发器，数据库
- [ ] 云端环境部署：在阿里云 FC 创建函数，并配置 `SILICONFLOW_API_KEY` 环境变量。
- [ ] 存储逻辑重构：将 `StorageClient` 适配为 **OSS SDK**，实现云端快照的持久化读写。
- [ ] 数据库落地：根据成员 B 的模型设计，建立指标状态表并实现 `ConflictDecision` 的结构化存库。
- [ ] 触发器配置：设置 **Cron 定时触发器**（如每 6 小时一次），并配置 `keyword` 参数入口。
### **成员 B：AI 逻辑引擎 (AI & Logic) —— 你的补完工作**
- [ ] 接入全局总结：在正式环境中启用 `generate_global_summary` 函数，输出首席分析师级别的总决策。
- [ ] 持久化接口对接：配合成员 A，确保 `resolve_conflicts` 输出的带置信度字段能正确对接数据库写入函数。
- [ ] 置信度动态化：将目前硬编码的置信度改为根据数据来源丰富度或 LLM 反馈动态计算。

### **成员 C：采集与前端展示 (Data & Frontend)**
关键：爬虫，报告开发
- [ ] 采集源标准化：完善 `ScraperAgent`，确保抓取的每条资讯都带有准确的 `SourceType` 标签（官方/媒体/传闻）。
- [ ] 动态报告开发：实现前端页面的动态渲染，首屏高亮展示成员 B 提供的 `global_summary`。
- [ ] 视觉高亮渲染：根据成员 B 输出的 `status` 字段（increased/decreased），实现红绿配色或趋势图标显示。

---

## 🏆 成员 B 已完成清单 (Completed)

你已经完成了整个智能体最核心的“处理中枢”，逻辑已具备生产就绪水平：
- [√] 语义化增量分析：实现了跨行业（半导体、新能源等）的动态指标识别与对齐。
- [√] 通俗化建议包装：通过 `insight` 字段将枯燥数值转化为通俗易懂的行业分析建议。
- [√] 自动化冲突仲裁：建立了基于来源权重的决策模型，并能输出最终结论及待核实来源。
- [√] JSON 解析容错：编写了 `robust_json_parse`，解决了 LLM 输出格式不规范导致的崩溃风险。
- [√] 全局决策聚合：设计了聚合多点变动并生成行业总定调的逻辑。
